Linux Environment Verification Report

1. Username and groups
   command: id 
   output:uid=1000(codespace) gid=1000(codespace) groups=1000(codespace),985(pipx),986(python),987(oryx),988(golang),989(docker),990(sdkman),
          991(rvm),992(php),993(conda),994(nvs),995(nvm),996(hugo),1001(ssh).

   Observation: 
   This command's output shows the current user is codespaces with UID 1000 and  GID(group ID) 1000. The User belongs to multiple groups such as 
   docker, php, python, golang, oryx, conda, nvm, and pipx provides access to development and administration tools.Here. The user's identity and 
   groups associates were successfully Verified using the <id> command

2. Current Shell 
   command: echo $SHELL
   output:  /bin/
   
   observatuon: The command <echo $SHELL> shows the current default shell used by the user account and The output will be </bin/bash> which is the 
   most used command-line interpreters. This means I am using Bash shell.It is confirmed that my session uses Bash.

3. Current Working Directory
   command: pwd
   output: /workspaces/Linux-Project/Question_1

   observation: This command <pwd> displays the absolute path of current working directory. The output displays the user currently working 
   in </workspaces/Linux-Project/Question_1>, this verified user current workspace's Location.

4. List of Files and Directory in my workspace
   command: ls -la
   output: total 72
           drwxrwxrwx+ 2 codespace codespace  4096 Jun 23 10:36 .
           drwxrwxrwx+ 4 codespace root       4096 Jun 23 09:39 ..
           -rw-rw-rw-  1 codespace codespace  1346 Jun 23 10:42 Enviroment_report.txt
           -rw-rw-rw-  1 codespace codespace 14541 Jun 23 09:59 Shell.png
           -rw-rw-rw-  1 codespace codespace 27373 Jun 23 09:47 id.png
           -rw-rw-rw-  1 codespace codespace 14926 Jun 23 10:35 workingdir.png

   observation: The command <ls -la> displays the all files including hidden items, files information.The output shows that this directory contains
   one Report file named as <Enviroment_report.txt> and three image files named as <Shell.png>, <workingdir.png> and <id.png>. it also displays 
   permission, sizes, owenrship and modification dates of the file.

5. Network Connectivity Verification
   command: ping -c 4 google.com
   output:  PING google.com (142.251.42.238) 56(84) bytes of data.
            --- google.com ping statistics ---
           4 packets transmitted, 0 received, 100% packet loss, time 3061ms
   
   Observation: This command <ping -c 4 google.com> executes for verify network connectivity.The output shows that 4 ICMP echo request packets 
   were sent. 0 ICMP echo request packets were received, resulting in 100 percent packet loss, for the ICMP echo request packets.That means the 
   network connectivity check was not successful and the system  not able to reach Googles server. The network is not working properly so the system 
   can not connect to Googles server. This is a problem, with the network connectivity.
            
           





   
